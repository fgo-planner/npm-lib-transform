/**
 * Atlas Academy `Attribute` enum values used by the `attribute` property in
 * the `NiceServant` and `BasicServant` data schemas.
 */
export type Attribute =
    'human' |
    'sky' |
    'earth' |
    'star' |
    'beast';
