/**
 * Atlas Academy `NiceItemBGType` enum values used by the `background` property
 * in the `NiceItem` data schema.
 */
export type NiceItemBGType =
    'zero' |
    'bronze' |
    'silver' |
    'gold' |
    'questClearQPReward';
